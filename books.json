---
layout: null
---
{ 
    {% for book in site.books %}
    "{{ book.asin }}": {
    "title": "{{ book.title }}",
    "asin": "{{ book.asin }}",
    "ref": "{{ site.amazon_tracking_id }}",
    "imgur-sm": "{{ book.imgur-sm }}"
    }{% if forloop.last == false %},{% endif %}
    {% endfor %}
}
